<?php

declare(strict_types=1);

/**
 * Шаблон без секретов. Скопируйте в **config.local.php** и вставьте токен там.
 * Рантайм читает **только** `config.local.php` в этом каталоге; переменные окружения не используются.
 * Этот файл с суффиксом `.example` **не подключается** PHP.
 */
return [
    'telegram_bot_token' => '',
];
